
module.exports = {
  local: require('./contract.defs.local'),
  pilot: require('./contract.defs.pilot'),
  poa: require('./contract.defs.poa'),
  test: require('./contract.defs.test'),
  tobalaba: require('./contract.defs.tobalaba')
}
